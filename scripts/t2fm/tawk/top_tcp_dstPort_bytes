#!/usr/bin/env awk

hdr() {
    next
}

!tcp() {
    next
}

# A flows
$dir == "A" && !bitsallset($flowStat, 1) {
    aggr($dstPort, $l7BytesRcvd, n)
}

# B flows
$dir == "A" && bitsallset($flowStat, 1) {
    aggr($srcPort, $l7BytesSnt, n)
}
