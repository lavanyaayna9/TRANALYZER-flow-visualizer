# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for pktSIATHisto
#
# Apply the configuration as follows:
#     - t2conf pktSIATHisto --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'pktSIATHisto.config':
#           t2conf pktSIATHisto -g
#     2. Edit 'pktSIATHisto.config'
#     3. Apply 'pktSIATHisto.config':
#           t2conf pktSIATHisto -C pktSIATHisto.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., pktSIATHisto,
#           e.g., src/pktSIATHisto.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: pktSIATHisto                                                          #
# ---------------------------------------------------------------------------- #

# Multiplication factor for red-black tree nodepool [1 - 255]
#   sizeof(nodepool) = PSIAT_NDPLF * mainHashMap->hashChainTableSize
PSIAT_NDPLF = 17

# Print histo in flow file [yes / no]
PRINT_HISTO = yes

# FIXME description is not clear:
# (Example: Bin = 10 -> iat = [50:55) -> min(iat) = 50ms)
#   0: Minimum of assigned inter arrival time
#   1: Bin number
HISTO_PRINT_BIN = 0

# Include (PSI_XMIN, UINT16_MAX] [yes / no]
PSI_XCLD = no

# Minimal packet length starts at PSI_XMIN [0 - 255]
# (require PSI_XCLD = 1)
PSI_XMIN = 1

# FIXME description is not clear [0 - 255]
#   > 1: modulo factor of packet length
PSI_MOD = 0

# Maximal number of section in statistics [1 - 255]
# (last section comprises all elements > IATBINBuN)
IATSECMAX = 3

# Destroy tree information after t2OnFlowTerminate() [yes / no]
# Do NOT switch on when dependent plugin, such as descriptiveStats is loaded!!
HISTO_EARLY_CLEANUP = 0

# Enable debug output [yes / no]
HISTO_DEBUG = no

# ---------------------------------------------------------------------------- #
# :from: ../../utils/default.config :import:                                   #
#   - RBT_ROTATION                                                             #
# ---------------------------------------------------------------------------- #
