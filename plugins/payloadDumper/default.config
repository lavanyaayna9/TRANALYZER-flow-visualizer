# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for payloadDumper
#
# Apply the configuration as follows:
#     - t2conf payloadDumper --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'payloadDumper.config':
#           t2conf payloadDumper -g
#     2. Edit 'payloadDumper.config'
#     3. Apply 'payloadDumper.config':
#           t2conf payloadDumper -C payloadDumper.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., payloadDumper,
#           e.g., src/payloadDumper.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: payloadDumper                                                         #
# ---------------------------------------------------------------------------- #

# Extract payload for layer 2 flows [yes / no]
# (require ETH_ACTIVATE > 0)
PLDUMP_L2 = no

# Only extract L2 payloads for those ethertypes, e.g., {0x2000,0x2003}
# (require PLDUMP_L2 = yes)
PLDUMP_ETHERTYPES = {}

# Extract payload for TCP flows [yes / no]
PLDUMP_TCP = yes

# Only extract TCP payloads on those ports, e.g., {80,8080}
# (require PLDUMP_TCP = yes)
PLDUMP_TCP_PORTS = {}

# Extract payload for UDP flows [yes / no]
PLDUMP_UDP = yes

# Only extract UDP payloads on those ports, e.g., {80,8080}
# (require PLDUMP_UDP = yes)
PLDUMP_UDP_PORTS = {}

# Extract payload for SCTP flows [yes / no]
# (require SCTP_ACTIVATE > 0)
PLDUMP_SCTP = no

# Only extract SCTP payloads on those ports, e.g., {80,8080}
# (require PLDUMP_SCTP = yes)
PLDUMP_SCTP_PORTS = {}

# Max. number of bytes per flow to dump (use 0 for no limits) [0 - UINT32_MAX]
PLDUMP_MAX_BYTES = 0

# Start dumping bytes at a specific offset (Layer 2 and UDP only) [0 - UINT32_MAX]
# (require PLDUMP_L2 = yes || PLDUMP_UDP = yes)
PLDUMP_START_OFF = 0

# Empty PLDUMP_FOLDER before starting [yes / no]
PLDUMP_RMDIR = yes

# Format for filenames:
#   0: flowInd '_' [AB]
#   1: srcIP.srcPort-dstIP.dstPort-l4Proto[_sctpStream],
#      srcMac-dstMac-etherType
#   2: Same as 1, but prefixed with timestampT
PLDUMP_NAMES = 0

# Output folder for saved files
PLDUMP_FOLDER = "/tmp/payloadDumper"

# Prefix for output files
PLDUMP_PREFIX = ""

# Suffix for output files
PLDUMP_SUFFIX = ""
