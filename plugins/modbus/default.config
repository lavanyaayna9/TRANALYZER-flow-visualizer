# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for modbus
#
# Apply the configuration as follows:
#     - t2conf modbus --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'modbus.config':
#           t2conf modbus -g
#     2. Edit 'modbus.config'
#     3. Apply 'modbus.config':
#           t2conf modbus -C modbus.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., modbus,
#           e.g., src/modbus.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: modbus                                                                #
# ---------------------------------------------------------------------------- #

# Print debug messages [yes / no]
MB_DEBUG = 0

# Function/Exception codes representation:
#   0: hexadecimal
#   1: integer
MB_FE_FRMT = 0

# Number of function codes to store [0 - 255]
# (0 to hide modbusFC)
MB_NUM_FUNC = 0

# Aggregate multiply defined function codes [yes / no]
MB_UNIQ_FUNC = 0

# Number of function codes which caused exceptions to store [0 - 255]
# (0 to hide modbusFEx)
MB_NUM_FEX = 0

# Aggregate multiply defined function codes which caused exceptions [yes / no]
MB_UNIQ_FEX = 0

# Number of exception codes to store [0 - 255]
# (0 to hide modbusExC)
MB_NUM_EX = 0

# Aggregate multiply defined exception codes [yes / no]
MB_UNIQ_EX = 0
