# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for sshDecode
#
# Apply the configuration as follows:
#     - t2conf sshDecode --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'sshDecode.config':
#           t2conf sshDecode -g
#     2. Edit 'sshDecode.config'
#     3. Apply 'sshDecode.config':
#           t2conf sshDecode -C sshDecode.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., sshDecode,
#           e.g., src/sshDecode.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: sshDecode                                                             #
# ---------------------------------------------------------------------------- #

# Count all packets to/from port SSH_PORT (22) as SSH [yes / no]
SSH_USE_PORT = no

# FIXME add a description:
#   0: Do not decode SSH handshake messages
#   1: Only decode SSH Key Exchange Init messages
#   2: Decode all SSH Exchange messages
SSH_DECODE = 2

# Algorithm to use for the fingerprint:
#   0: No fingerprint
#   1: MD5
#   2: SHA256
# (require SSH_DECODE = 2)
SSH_FINGERPRINT = 1

# Output chosen algorithms [yes / no]
SSH_ALGO = yes

# Output lists of supported algorithms [yes / no]
SSH_LISTS = no

# Output HASSH fingerprint (hash and description) [yes / no]
SSH_HASSH = yes

# Also output HASSH fingerprint before hashing [yes / no]
SSH_HASSH_STR = no

# Max length for HASSH descriptions [0 - 1024]
SSH_HASSH_DLEN = 512

# Max length for uncompressed HASSH signatures [0 - 2048]
SSH_HASSH_STR_LEN = 1024

# Name of the HASSH database
SSH_HASSH_NAME = "hassh_fingerprints.tsv"

# Max length for strings [0 - 4096]
SSH_BUF_SIZE = 512

# Max length for host key type and chosen algorithms [0 - 255]
SSH_HKT_SIZE = 48

# Activate debug output [yes / no]
SSH_DEBUG = no
