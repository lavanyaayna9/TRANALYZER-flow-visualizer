# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for pwX
#
# Apply the configuration as follows:
#     - t2conf pwX --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'pwX.config':
#           t2conf pwX -g
#     2. Edit 'pwX.config'
#     3. Apply 'pwX.config':
#           t2conf pwX -C pwX.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., pwX,
#           e.g., src/pwX.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: pwX                                                                   #
# ---------------------------------------------------------------------------- #

# Output the username [yes / no]
PWX_USERNAME = yes

# Output the password [yes / no]
PWX_PASSWORD = yes

# Extract FTP authentication [yes / no]
PWX_FTP = yes

# Extract POP3 authentication [yes / no]
PWX_POP3 = yes

# Extract IMAP authentication [yes / no]
PWX_IMAP = yes

# Extract SMTP authentication [yes / no]
PWX_SMTP = yes

# Extract HTTP Basic Authorization [yes / no]
PWX_HTTP_BASIC = yes

# Extract HTTP Proxy Authorization [yes / no]
PWX_HTTP_PROXY = yes

# Extract HTTP GET authentication [yes / no]
PWX_HTTP_GET = yes

# Extract HTTP POST authentication [yes / no]
PWX_HTTP_POST = yes

# Extract IRC authentication [yes / no]
PWX_IRC = yes

# Extract Telnet authentication [yes / no]
PWX_TELNET = yes

# Extract LDAP bind request authentication [yes / no]
PWX_LDAP = yes

# Extract PAP (Password Authentication Protocol) authentication [yes / no]
PWX_PAP = yes

# Extract authentication status (success, error, ...) [yes / no]
PWX_STATUS = yes

# Activate debug output [yes / no]
PWX_DEBUG = no
