# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for telnetDecode
#
# Apply the configuration as follows:
#     - t2conf telnetDecode --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'telnetDecode.config':
#           t2conf telnetDecode -g
#     2. Edit 'telnetDecode.config'
#     3. Apply 'telnetDecode.config':
#           t2conf telnetDecode -C telnetDecode.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., telnetDecode,
#           e.g., src/telnetDecode.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: telnetDecode                                                          #
# ---------------------------------------------------------------------------- #

# Save content to TEL_F_PATH [yes / no]
TEL_SAVE = no

# Empty TEL_F_PATH before starting [yes / no]
# (prevent appending to existing files)
# (require TEL_SAVE = yes)
TEL_RMDIR = yes

# Save requests (A) and responses (B):
# (require TEL_SAVE = yes)
#    0: in the same file
#    1: in separate files
TEL_SAVE_SPLIT = 1

# FIXME description is not clear:
# (require TEL_SAVE = yes)
#    0: no file position control
#    1: seq number file position control
TEL_SEQPOS = 0

# Enable bitfields output [yes / no]
TEL_BTFLD = yes

# Commands/options format:
#    1: Output commands/options names
#    0: Output commands/options
TEL_CMDOPTS = 1

# Aggregate commands [yes / no]
TEL_CMD_AGGR = yes

# Aggregate options [yes / no]
TEL_OPT_AGGR = yes

# Maximal length user/password [0 - 255]
TELUPLN = 25

# Maximal command / flow [0 - 255]
TELCMDN = 25

# Maximal options / flow [0 - 255]
TELOPTN = 25

# Path for extracted content
TEL_F_PATH = "/tmp/TELFILES/"
