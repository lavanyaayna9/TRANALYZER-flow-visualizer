# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for nFrstPkts
#
# Apply the configuration as follows:
#     - t2conf nFrstPkts --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'nFrstPkts.config':
#           t2conf nFrstPkts -g
#     2. Edit 'nFrstPkts.config'
#     3. Apply 'nFrstPkts.config':
#           t2conf nFrstPkts -C nFrstPkts.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., nFrstPkts,
#           e.g., src/nFrstPkts.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: nFrstPkts                                                             #
# ---------------------------------------------------------------------------- #

# Time stamp modes for packet signal
#   0: Time relative to flow start
#   1: Inter-arrival time
#   2: Absolute time
NFRST_IAT = 1

# Pulse Signal preparation for NFRST_IAT=0 mode
# Shift beginning of B Signal relative to A Signal
#   0: B Signal not shifted
#   1: B Signal shifted by B-A Flow timeFirst difference
NFRST_BCORR = 0

# Pulse Mode
# Minimal IAT sec to define a pulse [0 - UINT32_MAX]
NFRST_MINIATS = 0

# Pulse Mode
# Minimal IAT usec to define a pulse [0 - UINT32_MAX]
NFRST_MINIATU = 0

# Pulse Mode: Minimal pulse length fraction [0 - 255]
NFRST_MINPLENFRC = 2

# Pulse mode: Amplitude of aggregated pulse
#   0: Sum(PL) (BPP) (require NFRST_MINIATS > 0 || NFRST_MINIATU > 0)
#   1: Packet length average
NFRST_PLAVE = 1

# Define how many first packets are recorded [0 - 255]
NFRST_PKTCNT = 20

# Output L3 and L4 header length [yes / no]
NFRST_HDRINFO = no

# Min PL boundary [0 - NFRST_XMAX]
# (require NFRST_XCLD = 1)
NFRST_XMIN = 0

# Max PL boundary [NFRST_XMIN - UINT16_MAX]
# (require NFRST_XCLD = 1)
NFRST_XMAX = UINT16_MAX
