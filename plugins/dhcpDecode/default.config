# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for dhcpDecode
#
# Apply the configuration as follows:
#     - t2conf dhcpDecode --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'dhcpDecode.config':
#           t2conf dhcpDecode -g
#     2. Edit 'dhcpDecode.config'
#     3. Apply 'dhcpDecode.config':
#           t2conf dhcpDecode -C dhcpDecode.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., dhcpDecode,
#           e.g., src/dhcpDecode.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: dhcpDecode                                                            #
# ---------------------------------------------------------------------------- #

# Options representation:
#   0: bitfield
#   1: numbers
#   2: names
DHCPMOTOUT = 1

# Maximum stored options [1 - 255]
# (require DHCPMOTOUT > 0)
DHCPOPTMAX = 20

# Maximum stored message types [1 - 255]
# (require DHCPMOTOUT > 0)
DHCPMSGMAX = 20

# Maximal number of domain/host names per flow [1 - 255]
# (require DHCPMOTOUT > 0)
DHCPNMMAX = 10

# Netmask representation:
#   0: hexadecimal
#   1: IP
DHCPMASKFRMT = 1

# Print the number of times a given mac/domain/host appeared [yes / no]
DHCP_ADD_CNT = no

# Store a global mapping IP->MAC and add the source and
# destination MAC address to every flow [yes / no]
# (EXPERIMENTAL, IPv4 only)
DHCP_FLAG_MAC = no

# Print debug information about DHCP_FLAG_MAC operations [yes / no]
DHCP_FM_DEBUG = no
