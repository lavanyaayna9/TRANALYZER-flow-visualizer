# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for bgpDecode
#
# Apply the configuration as follows:
#     - t2conf bgpDecode --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'bgpDecode.config':
#           t2conf bgpDecode -g
#     2. Edit 'bgpDecode.config'
#     3. Apply 'bgpDecode.config':
#           t2conf bgpDecode -C bgpDecode.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., bgpDecode,
#           e.g., src/bgpDecode.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: bgpDecode                                                             #
# ---------------------------------------------------------------------------- #

# Activate debug output [yes / no]
BGP_DEBUG = no

# Format for IP addresses:
#   0: hex
#   1: IP
#   2: int
BGP_IP_FORMAT = 1

# Format for AS numbers:
#   0: ASPLAIN,
#   1: ASDOT,
#   2: ASDOT+
BGP_AS_FORMAT = 0

# Format for notifications:
#   0: uint8,
#   1: string (code only)
BGP_NOTIF_FORMAT = 0

# Flag traditional bogons [yes / no]
BGP_TRAD_BOGONS = yes

# Output routing tables [yes / no]
BGP_OUTPUT_RT = yes

# Output originator id [yes / no]
# (require BGP_OUTPUT_RT = yes)
BGP_ORIG_ID = no

# Output aggregator [yes / no]
# (require BGP_OUTPUT_RT = yes)
BGP_AGGR = no

# Output cluster list [yes / no]
# (require BGP_OUTPUT_RT = yes)
BGP_CLUSTER = no

# Output communities [yes / no]
# (require BGP_OUTPUT_RT = yes)
BGP_COMMUNITIES = no

# Format for network mask:
#   0: hex
#   1: IP
#   2: int
# (require BGP_OUTPUT_RT = yes)
BGP_MASK_FORMAT = 1

# Aggregate repetitions of the same AS [yes / no]
# (require BGP_OUTPUT_RT = yes)
BGP_AS_PATH_AGGR = 0

# Size of arrays for update records [0 - UINT16_MAX]
BGP_ASIZE = 512

# Suffix for output file
BGP_SUFFIX = "_bgp.txt"

# Suffix for multiple origin AS file
BGP_MOAS_SUFFIX = "_bgp_moas.txt"

# Suffix for anomaly file
BGP_ANOM_SUFFIX = "_bgp_anomalies.txt"

# [!!! -------------------------------------------------------------------- !!!]
# [!!! -------------- The following options are experimental -------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]

# Store routing information in a hashtable [yes / no]
# (required for MOAS detection)
BGP_RT = yes

# Activate debug output for routing information [yes / no]
BGP_DEBUG_RT = no

# Use the mask as part of the key for the routing table [yes / no]
BGP_RT_MASK = no
