# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for bitForensic
#
# Apply the configuration as follows:
#     - t2conf bitForensic --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'bitForensic.config':
#           t2conf bitForensic -g
#     2. Edit 'bitForensic.config'
#     3. Apply 'bitForensic.config':
#           t2conf bitForensic -C bitForensic.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., bitForensic,
#           e.g., src/bitForensic.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: bitForensic                                                           #
# ---------------------------------------------------------------------------- #

# 0: Pattern search
# 1: Length
BF_PLEN = 0

# 0: Residual length
# 1: Total length
BF_TOTLEN = 0

# Length excluded from pattern length [0 - 65535]
BF_EXLEN = 2

# Pattern: 1,2,4,8 bytes, defines BF_PWDTH
BF_PAT = 0x0915

# Mask: 1,2,4,8 bytes, defines BF_PWDTH
BF_MSK = 0xffff

# 0: Host order
# 1; Network order
BF_NETODR = yes

# Swap nibbles in search pattern [yes / no]
BF_NIBBLESWP = no

# Max bPDPos flow storage [1 - UINT32MAX]
BF_DNUM = 10

# Save B/D info [yes / no]
BF_SAVE_BCH = no

# Extract content: start byte shift [0 - 65535]
# (require BF_SAVE_BCH == 1)
BF_BSHIFT = 0

# Empty BF_V_PATH before starting [yes / no]
BF_RMDIR = yes

# Path for raw content
BF_V_PATH = "/tmp/BF/"

# Default content file name prefix
BF_FNAME = "bfnudel"
