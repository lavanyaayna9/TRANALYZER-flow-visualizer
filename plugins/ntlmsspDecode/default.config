# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for ntlmsspDecode
#
# Apply the configuration as follows:
#     - t2conf ntlmsspDecode --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'ntlmsspDecode.config':
#           t2conf ntlmsspDecode -g
#     2. Edit 'ntlmsspDecode.config'
#     3. Apply 'ntlmsspDecode.config':
#           t2conf ntlmsspDecode -C ntlmsspDecode.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., ntlmsspDecode,
#           e.g., src/ntlmsspDecode.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: ntlmsspDecode                                                               #
# ---------------------------------------------------------------------------- #

# Output client challenge [yes / no]
NTLMSSP_CLI_CHALL = no

# Output DNS computer/domain/tree name [yes / no]
NTLMSSP_DNS = yes

# Output NetBIOS computer/domain name [yes / no]
NTLMSSP_NETBIOS = yes

# Extract NetNTLMv1 hashes [yes / no]
NTLMSSP_SAVE_AUTH_V1 = yes

# Extract NetNTLMv2 hashes [yes / no]
NTLMSSP_SAVE_AUTH_V2 = yes

# Add flow information in the hashes files [yes / no]
# (require NTLMSSP_SAVE_AUTH_V1 = yes || NTLMSSP_SAVE_AUTH_V2 = yes)
NTLMSSP_SAVE_INFO = no

# Output format for the version:
#   0: do not output the version
#   1: output the version as string
#   2: output the version as major_minor_build_rev
NTLMSSP_VERSION = 2

# Suffix for NetNTLMv1 hashes file
# (require NTLMSSP_SAVE_AUTH_V1 = yes)
NTLMSSP_AUTH_V1_FILE = "_NetNTLMv1.txt"

# Suffix for NetNTLMv2 hashes file
# (require NTLMSSP_SAVE_AUTH_V2 = yes)
NTLMSSP_AUTH_V2_FILE = "_NetNTLMv2.txt"

# Max length for string output [1 - 255]
NTLMSSP_NAME_LEN = 64
