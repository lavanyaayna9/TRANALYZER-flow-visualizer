# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for regex_pcre
#
# Apply the configuration as follows:
#     - t2conf regex_pcre --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'regex_pcre.config':
#           t2conf regex_pcre -g
#     2. Edit 'regex_pcre.config'
#     3. Apply 'regex_pcre.config':
#           t2conf regex_pcre -C regex_pcre.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., regex_pcre,
#           e.g., src/regex_pcre.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: regex_pcre                                                            #
# ---------------------------------------------------------------------------- #

# FIXME add a description:
#   0: only display the most severe class,
#   1: display all matched classes plus some extra information
EXPERTMODE = 0

# Display the time at which a rule was matched [yes / no]
PKTTIME = no

# Aggregate alarms [yes / no]
AGGR = no

# Enable sending FL_ALARM for pcapd [yes / no]
SALRMFLG = no

# Regex internal: maximal number of regex output vectors [0 - 255]
#OVECCOUNT = 3

# Max number of matches stored / flow [0 - 255]
MAXREGPOS = 30

# Regex file name
RGX_POSIX_FILE = "regexfile.txt"

# ---------------------------------------------------------------------------- #
# :file: src/regfile_pcre.h                                                    #
# ---------------------------------------------------------------------------- #

# Allocate opt rule structure & compile regex [yes / no]
RULE_OPTIMIZE = yes

# regex compile time options
REGEX_MODE = PCRE_DOTALL

# Max number of node predecessors [0 - 255]
PREIDMX = 4
