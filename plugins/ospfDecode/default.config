# [!!! -------------------------------------------------------------------- !!!]
# [!!! ---------------------- DO NOT EDIT THIS FILE ----------------------- !!!]
# [!!! -------------------------------------------------------------------- !!!]
#
# This is the default configuration for ospfDecode
#
# Apply the configuration as follows:
#     - t2conf ospfDecode --reset
#
# If you want to customize the default configuration, proceed as follows:
#     1. Create 'ospfDecode.config':
#           t2conf ospfDecode -g
#     2. Edit 'ospfDecode.config'
#     3. Apply 'ospfDecode.config':
#           t2conf ospfDecode -C ospfDecode.config
#
# [Format of the file]
#     - Empty lines and lines starting with '%' or '#'’ are ignored.
#     - One section starting with '# :name: pluginName" per file.
#     - If no section starting with '# :file: path/to/file.h" is specified,
#       it is assumed the file is src/pluginName.h (relative to plugin folder)
#         - If a configuration element applies to a different file, a file
#           section MUST be used
#         - Filenames are relative to plugin folder, i.e., ospfDecode,
#           e.g., src/ospfDecode.h
#     - A line is composed of the name of a macro (define) in the header file
#       followed by an equal sign and the value to set for the define:
#           NAME = value
#       where value can be anything, including yes (= 1) and no (= 0)
#     - Add a comment before each setting to describe the configuration option
#       and its valid values:
#           # Description of the option [yes / no]
#       or
#           # Description of the option [0 - 255]
#       or
#           # Description of the option:
#           #   0: description of value 0
#           #   1: description of value 1
#           #   2: description of value 2

# ---------------------------------------------------------------------------- #
# :name: ospfDecode                                                            #
# ---------------------------------------------------------------------------- #

# Output hello messages [yes / no]
OSPF_OUTPUT_HLO = yes

# Output database description messages (routing tables) [yes / no]
OSPF_OUTPUT_DBD = yes

# Output all other messages [yes / no]
OSPF_OUTPUT_MSG = yes

# Output statistics file [yes / no]
OSPF_OUTPUT_STATS = yes

# Netmasks representation:
#   0: Hexadecimal
#   1: IPv4 address
OSPF_MASK_AS_IP = 1

# Areas representation:
#   0: Integer
#   1: IPv4 address
#   2: Hexadecimal
OSPF_AREA_AS_IP = 0

# Link State ID representation:
#   0: Integer
#   1: IPv4 address
OSPF_LSID_AS_IP = 1

# Message type representation:
#   0: Aggregated hexadecimal bitfield
#   1: List of strings
OSPF_TYP_STR = 1

# LS type representation:
#   0: Integer
#   1: String
OSPF_LSTYP_STR = 1

# Maximum number of neighbors to store [0 - 255]
OSPF_NEIGMAX = 10

# Maximum number of LS types to store [0 - 255]
# (require OSPF_TYP_STR = 1)
OSPF_NUMTYP = 10

# Suffix for OSPF output file
OSPF_SUFFIX = "_ospfStats.txt"

# Suffix for OSPF hello messages
OSPF_HELLO_SUFFIX = "_ospfHello.txt"

# Suffix for OSPF database description (routing tables)
OSPF_DBD_SUFFIX = "_ospfDBD.txt"

# Suffix for all other messages from OSPFv2 (Link State Request/Update/Ack)
OSPF2_MSG_SUFFIX = "_ospf2Msg.txt"

# Suffix for all other messages from OSPFv3 (Link State Request/Update/Ack)
OSPF3_MSG_SUFFIX = "_ospf3Msg.txt"
