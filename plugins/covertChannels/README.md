covertChannels Tranalyzer2 plugin
==================================

Debug mode
----------

```
$ ./autogen.sh -d
```

In debug mode, this plugin provides additional information on `stderr` when a covert channel is detected.
